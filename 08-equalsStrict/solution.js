/*
Problem:  Whats the difference between the comparison operators == and === ?
*/

// Equals
if ("0" == 0) console.log(true) // Returns true because only compare the values


// Strict Equals
if ("0" === 0) console.log(true) // Returns false because it compares the values and the types